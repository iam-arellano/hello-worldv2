apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-the-town
spec:
  rules:
  - host: "my.domain"         # domain
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: service-the-town
            port:
              number: 80
      - path: "/helloworld2"
        pathType: Prefix
        backend:
          service:
            name: service-helloworld2
            port:
              number: 80